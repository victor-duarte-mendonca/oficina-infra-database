# oficina-infra-database

RDS PostgreSQL gerenciado (Terraform) — Tech Challenge FIAP, Fase 3.

Repositório criado na etapa E0 a partir do histórico de `oficina/infra/rds.tf` do monorepo.
Providers, backend, variáveis, subnet group, parameter group e SG próprios entram em E3 (spec 07).
